css چیست؟
Cascade Style Sheet
چرا css

۱. امکاناتی دارد که HTML ندارد.
۲. معادل‌های HTML‌ غیراستاندارد، در CSS، استاندارد می‌باشد.
۳. حجم صفحات را به شدت کاهش می‌دهد.
۴. تغییرات ظاهری در صفحه را به شکلی ساده و متمرکز امکان‌پذیر می‌کند.
۵. تغییرات ظاهری در کل سایت را به شکلی ساده و متمرکز امکان‌پذیر می‌کند.